[permissions]
allow = ["Bash(cd \"D:\\\\Documents\\\\New project\" && node -e \"\nvar deps = {\n  FREE_AUDIO_MANIFEST: { events: { movementIce: {} } },\n  clamp: function(x,a,b) { return Math.max(a, Math.min(b,x)); },\n  sharedState: { TILE: 16 },\n  requireRuntimeModule: function(){}\n};\nvar depsAliases = '';\nfor (var key in deps) {\n  if (deps.hasOwnProperty(key) && typeof deps[key] !== 'function' && key !== 'sharedState') {\n    depsAliases += 'var ' + key + ' = deps[\\\"' + key + '\\\"];';\n  }\n}\nconsole.log('Generated:', depsAliases);\neval(depsAliases);\nconsole.log('FREE_AUDIO_MANIFEST:', typeof FREE_AUDIO_MANIFEST);\n\")", "Bash(cd \"D:\\\\Documents\\\\New project\" && sed -i '\ns/state\\.stageRuntime\\.prepareBattleGrid/deps.prepareBattleGrid/g\ns/state\\.stageRuntime\\.prepareConstructedBattleGrid/deps.prepareConstructedBattleGrid/g\ns/state\\.stageRuntime\\.gameSettings() || {}/state.stageRuntime.gameSettings()/g\n' src/runtime/game-lifecycle.js && echo \"Fixed\" && grep -c \"state\\.stageRuntime\\.prepareBattleGrid\\|state\\.stageRuntime\\.prepareConstructedBattleGrid\" src/runtime/game-lifecycle.js)", "Bash(cd \"D:\\\\Documents\\\\New project\" && node tests/integration/audio-mix-rules.test.js 2>&1 | head -5)", "Bash(cd \"D:\\\\Documents\\\\New project\" && grep -E \"^\\s+[A-Z_]+:\" src/runtime/shared-state.js | sed 's/.*:\\s*\\([A-Z_]*\\).*/  var \\1 = sh.\\1;/' | head -50)", "Bash(cd \"D:\\\\Documents\\\\New project\" && python -c \"\nimport re\n\n# Read module-deps.js to get all export names\nwith open('src/runtime/module-deps.js', 'r', encoding='utf-8') as f:\n    content = f.read()\n\n# Find all return { ... } exports\n# Look for pattern: name: source.name,\nexports = re.findall(r'^\\s+(\\w+):\\s+\\w+\\.\\w+,?\\s*$', content, re.MULTILINE)\n\n# Generate var aliases\nfor name in sorted(set(exports)):\n    print('  var %s = deps.%s;' % (name, name))\n\" | head -60)", "Bash(cd \"D:\\\\Documents\\\\New project\" && python << 'PYEOF'\nimport re\n\n# Read module-deps.js exports\nwith open('src/runtime/module-deps.js', 'r', encoding='utf-8') as f:\n    deps_content = f.read()\n\n# Extract export names from the return block\n# The return block is: return { ... };\nreturn_match = re.search(r'return \\{([^}]+(?:}[^}]+)*)\\}', deps_content)\nif not return_match:\n    # Try broader match\n    return_match = re.search(r'return \\{([\\s\\S]+?)\\n  \\};', deps_content)\n\n# Find all top-level key: value, pairs (not nested)\nexports = set()\nfor line in deps_content.split('\\n'):\n    m = re.match(r'^\\s+(\\w+):\\s+[\\w.]+,\\s*$', line)\n    if m:\n        exports.add(m.group(1))\n\nprint(\"Total deps exports:\", len(exports))\n\n# Read game.js\nwith open('src/game.js', 'r', encoding='utf-8') as f:\n    game_content = f.read()\n\n# Find the Deps module aliases section\nold_section = game_content[game_content.find('  // Deps module aliases'):game_content.find('  function handleAction')]\n\n# Generate new aliases\n# Separate functions from values\nimports_funcs = set()\nimports_vals = set()\n\n# Check which exports are functions vs values by looking at the original module\nfor name in sorted(exports):\n    # Common function exports (based on naming convention and known deps)\n    if name[0].lower() == name[0]:  # camelCase = likely function\n        imports_funcs.add(name)\n    else:\n        imports_vals.add(name)\n\n# Generate alias block\nalias_block = '\\n  // Deps module aliases\\n'\nfor name in sorted(imports_vals):\n    alias_block += '  var %s = deps.%s;\\n' % (name, name)\nfor name in sorted(imports_funcs):\n    alias_block += '  var %s = deps.%s;\\n' % (name, name)\nalias_block += '\\n'\n\n# Replace in game.js\nnew_game = game_content.replace(old_section, alias_block)\n\nwith open('src/game.js', 'w', encoding='utf-8') as f:\n    f.write(new_game)\n\nprint(\"Game.js updated with %d deps aliases\" % len(exports))\nPYEOF)", "Bash(cd \"D:\\\\Documents\\\\New project\" && node tests/integration/audio-mix-rules.test.js 2>&1 | head -15)", "Bash(cd \"D:\\\\Documents\\\\New project\" && node tests/run-tests.js 2>&1 | grep -E \"passed|Error|failed\" | head -20)", "Bash(cd \"D:\\\\Documents\\\\New project\" && node --check src/game.js && node --check src/runtime/debug-api.js && node --check src/runtime/game-lifecycle.js && node --check src/runtime/audio-bridge.js && echo \"Syntax OK\" && node tools/_debug_browser.js 2>&1)", "Bash(cd \"D:\\\\Documents\\\\New project\" && rm -f tools/_debug_browser.js && git add -A && git diff --stat HEAD)"]
